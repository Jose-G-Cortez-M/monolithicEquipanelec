{% extends 'base.html.twig' %}

{% block title %}Edit Movement{% endblock %}

{% block body %}

    <div class="container h-100">
        <div class="row h-100 justify-content-center align-items-center">
            <div class="col-9 bg-white rounded my3 p-5">
                <h1>Edit Movement</h1>
                {{ include('movement/_form.html.twig', {'button_label': 'Update'}) }}
                {{ include('movement/_delete_form.html.twig') }}
                <a href="{{ path('movement_list') }}"><i class="fas fa-hand-point-left"> back to list</i></a>

            </div>
        </div>
    </div>
{% endblock %}
