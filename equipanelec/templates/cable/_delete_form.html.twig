<form method="post" action="{{ path('cable_delete', {'id': cable.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cable.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
