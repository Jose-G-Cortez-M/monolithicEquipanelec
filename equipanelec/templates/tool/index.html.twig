{% extends 'base.html.twig' %}

{% block title %}Tool List{% endblock %}

{% block body %}
    <h1>Tool List</h1>

    <h4><a href="{{ path('tool_new') }}"><i class="fas fa-tools"> Add new Tool</i></a></h4>
    <table class="table table-hover table-bordered">
        <thead>
            <tr class="table-dark">
                <th>Image</th>
                <th>Name</th>
                <th>Stock</th>
                <th>Location</th>
                <th>Description</th>
                <th>Minimum Limit</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for tool in tools %}
            {% if tool.minimumLimit is null or tool.minimumLimit>tool.stock %}
                <tr class="table-warning">
                    <td><img src="{{ vich_uploader_asset(tool, 'imageFile') }}" alt="{{ tool.name }}" height="100"></td>
                    <td>{{ tool.name }}</td>
                    <td>{{ tool.stock }}</td>
                    <td>{{ tool.location }}</td>
                    <td id="desc">{{ tool.description }}</td>
                    <td>{{ tool.minimumLimit }}</td>
                    <td>
                        <a href="{{ path('tool_show', {'id': tool.id}) }}"><i class="far fa-eye"><i class="far fa-eye"> Show details</i></i></a>
                        <a href="{{ path('tool_edit', {'id': tool.id}) }}"><i class="fas fa-edit"> Upgrade or add more tool</i></a>
                        {{ include('tool/_movement_form.html.twig') }}
                    </td>
            </tr>
            {% else %}
                <tr>
                    <td><img src="{{ vich_uploader_asset(tool, 'imageFile') }}" alt="{{ tool.name }}" height="100"></td>
                    <td>{{ tool.name }}</td>
                    <td>{{ tool.stock }}</td>
                    <td>{{ tool.location }}</td>
                    <td id="desc">{{ tool.description }}</td>
                    <td>{{ tool.minimumLimit }}</td>
                    <td>
                        <a href="{{ path('tool_show', {'id': tool.id}) }}"><i class="far fa-eye"> Show details</i></a>
                        <br>
                        <a href="{{ path('tool_edit', {'id': tool.id}) }}"><i class="fas fa-edit"> Upgrade or add more tool</i></a>
                        {{ include('tool/_movement_form.html.twig') }}
                    </td>
                </tr>
            {% endif %}
        {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
